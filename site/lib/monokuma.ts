/** Sentinela: não é um id de personagem real do elenco (Monokuma não é
 * jogável). Só o host de uma partida pode escolher esta opção, pro papel de
 * mestre/narrador da sessão. */
export const ID_MONOKUMA = 'monokuma';

/** Mesmo estilo dos sprites 8-bit do elenco (ver lib/sprites-pixel.ts). */
export const SPRITE_MONOKUMA = '/sprites/pixel/monokuma.png';
